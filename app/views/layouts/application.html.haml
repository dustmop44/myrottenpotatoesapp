!!! 5
%html
  - unless current_moviegoer
    =link_to "Sign in with Facebook", moviegoer_facebook_omniauth_authorize_path
  - else
    =link_to "Logout", destroy_moviegoer_session_path, method: :delete
  %head
    %h1 All Movies
    %title RottenPotatoes!
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    
  %body
    - if flash[:notice]
      #notice.message= flash[:notice]
    - elsif flash[:warning]
      #warning.message= flash[:warning]
    = yield